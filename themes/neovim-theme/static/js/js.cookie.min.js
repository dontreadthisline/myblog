// Minimal cookie helper - replaces js-cookie dependency
const Cookies = {
  get(key) {
    const v = document.cookie.match("(^|;) ?" + key + "=([^;]*)(;|$)");
    return v ? v[2] : null;
  },
  set(key, value) {
    document.cookie = key + "=" + value + ";path=/;SameSite=Lax";
  },
};
